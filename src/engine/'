#pragma once
#include "component.h"
#include <string>



namespace engine {


struct ScriptLiteral {
  std::string script;
};

struct ScriptFile {
  std::string script_path;
  std::string cached_script;
  bool stale;
};

class ScriptComponent : public Component {
public:
  virtual void tick(float dt) override;

private:
  std::variant<ScriptLiteral, ScriptFile> script;

};
}
